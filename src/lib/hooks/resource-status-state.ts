/**
 * @todo(mp) Expose `ResourceStatus_State` in the ts-sdk and remove.
 * Mirrors `viam.robot.v1.ResourceStatus.State`.
 */
export const STATE_UNCONFIGURED = 1;
export const STATE_CONFIGURING = 2;
export const STATE_REMOVING = 4;
