---
'@viamrobotics/svelte-sdk': minor
---

Refetch `resourceNames` when a resource registers or disappears, so a remote or module that comes up after the machine reports running no longer stays invisible until the page reloads.
